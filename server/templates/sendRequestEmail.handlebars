Hello {{recipient.username}},

{{sender.username}} has sent you a request for {{currencyAbbr}}{{request.amount}} for: "{{request.description}}" on {{siteName}}!

To fulfill this request and send {{sender.username}} {{currencyAbbr}}{{request.amount}}, just go to {{server}}/send/{{sender.username}}/{{request.amount}}